#include "main.h"
/**
 * _islower - check for lowercase character
 * @c: is the int used in function argument
 * Return: 0
 */
int _islower(int ci)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
		return (0);
}
